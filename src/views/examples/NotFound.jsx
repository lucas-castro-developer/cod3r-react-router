import React from "react";

const NotFound = (props) => (
  <div className="NotFound">
    <h1>Página não encontrada!</h1>
    <h2>Opa! Acho que o conteúdo que você tenta encontrar não está disponível.</h2>
  </div>
);

export default NotFound;
